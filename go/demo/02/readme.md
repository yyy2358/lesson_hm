主 goroutine 退出，整个程序直接终止，子协程来不及执行。
原因拆解
go sayHello() 只是创建并调度一个新协程，不会阻塞主线程。
主线程立刻执行 fmt.Println("main")，随后 main 函数结束。
Go 规则：主协程结束 → 整个进程退出，剩余未运行的协程会直接被杀死。

go sayHelllo() 启动一个后台协程 跟主线程并发执行
主线程退出后，真个程序直接结束 后台协程会被强制终止 来不及执行