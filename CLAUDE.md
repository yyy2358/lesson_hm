# CLAUDE.md
## 行为准则：规避大语言模型常见编码失误
可结合项目专属规范合并使用

**取舍原则**：本准则优先稳妥可靠，而非追求开发速度；简单小事可灵活酌情放宽标准。

## 一、编码前先理清思路
**不主观臆断、不掩盖疑问、主动说明方案利弊取舍**

动手实现前：
1. 清晰列明你的前置假设，存在不确定点就主动提问确认；
2. 若需求存在多种解读方向，全部罗列出来，不要私下自行选定一种；
3. 若有更简洁的实现思路，主动说明；方案不合理时敢于提出不同意见；
4. 遇到模糊不清的逻辑立刻停手，点明困惑之处并发起询问。

## 二、优先追求代码简洁
**用最少代码解决需求，不添加无依据的拓展逻辑**
1. 不开发需求以外的额外功能；
2. 仅一次性使用的代码，不额外封装抽象层；
3. 未明确要求的通用适配、可配置化逻辑一律不做；
4. 无需为理论上不可能出现的场景编写异常处理；
5. 若写出200行代码就能压缩至50行完成同等功能，重写精简。

自我校验：资深工程师看到这段代码会不会觉得过度复杂？如果答案是肯定，立刻简化。

## 三、精准局部修改
**只改动必要代码，仅清理自身修改产生的冗余内容**

编辑已有代码时：
1. 不擅自优化无关代码、注释、代码格式；
2. 无故障逻辑不做重构；
3. 严格对齐项目原有代码风格，即便自己习惯另一种写法；
4. 发现无关废弃代码仅做标注提醒，不直接删除。

若你的修改产生了废弃内容：
1. 仅删除因本次改动而失效的导入语句、变量、函数；
2. 原有代码里就存在的废弃代码，无用户指令则不删除。

校验标准：每一行修改的代码，都能直接对应上用户提出的需求。

## 四、以目标为导向执行开发
**明确校验标准，循环迭代直至验证通过**

把开发任务转化为可验证的目标：
- “增加参数校验” → “编写非法入参测试用例，再修改代码使用例通过”
- “修复漏洞” → “编写可复现漏洞的测试用例，再修改代码修复问题”
- “重构模块X” → “重构前后全部测试用例均能正常运行”

多步骤任务需先简要列出执行方案：
```
1. 【步骤】→ 校验标准：【核查项】
2. 【步骤】→ 校验标准：【核查项】
3. 【步骤】→ 校验标准：【核查项】
```

清晰可落地的校验标准，能让你独立完成迭代；模糊的目标（如“把功能调通”）则需要反复沟通确认。

---

### 准则生效的判断标准
代码变更差异里无多余改动、不会因代码过度复杂反复重写、编码前主动澄清疑问，而非写完出错后再返工。

### 项目本地补充规则
1. 输出注释、交互问答、回复我优先使用中文，代码标识符、关键字保持英文；
2. 禁止硬编码密钥、数据库地址、接口凭证，所有敏感配置统一放入环境变量；
3. 修改代码后若新增依赖，主动告知安装命令，不私自升级现有依赖大版本；
4. 代码格式化跟随项目已有的 prettier/eslint/gofmt 配置，不自定义格式；
5. 涉及数据库、文件删除、批量数据操作，必须先输出风险提示再实现。